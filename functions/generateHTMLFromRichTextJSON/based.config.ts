import { BasedFunctionConfig } from '@based/functions'

const config: BasedFunctionConfig = {
  type: 'function',
  name: 'generate-html-from-rich-text-json',
  public: true,
}

export default config
